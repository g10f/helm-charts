{{- if .Values.ingressRoute.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "wiki.fullname" . }}-nginx
  labels:
    {{- include "wiki.labels" . | nindent 4 }}
data:
  default.conf: |
{{ .Values.nginx.config | indent 4 }}
{{- end }}
